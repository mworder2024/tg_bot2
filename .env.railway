# Railway Environment Template
# Copy these to Railway dashboard environment variables

# Required
BOT_TOKEN=your_telegram_bot_token_here
JWT_SECRET=generate_random_secret_here
NODE_ENV=production

# Optional (defaults are fine)
PORT=3000
LOG_LEVEL=info
RATE_LIMIT_TTL=60000
RATE_LIMIT_LIMIT=100
CORS_ORIGIN=*
JWT_EXPIRES_IN=24h

# Database (provided automatically by Railway)
# DATABASE_URL=postgresql://user:pass@host:port/db

# Webhook Configuration (optional - only if not using polling)
# BOT_WEBHOOK_URL=https://your-app.railway.app/api/v1/bot/webhook
# BOT_WEBHOOK_SECRET=generate_random_secret_here